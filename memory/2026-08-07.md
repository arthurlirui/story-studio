# 2026-08-07

## story-studio 复评 + 性能测试

- 测试修复完成：tests/test_auto_revise.py mock 改用 `你是 **角色名` 锚定，336/336 全绿。生产修订循环无 bug。
- LLM 端点 https://llmapi.pcl.ac.cn/v1 的 /models 列出 GLM-5.2 / DeepSeek-V4-Pro / Deepseek-v4-Flash，但该 token 只有 DeepSeek-V4-Pro 可用：GLM-5.2/Flash 按列出名调用返回 503 "No available channel"，其他大小写变体 403 "no access"。
- **此端点无 K3**；env 和仓库中也无 Kimi/Moonshot/K3 凭据。K3 性能测试无法进行，需要用户提供 K3 端点/密钥。
- 基准（DeepSeek-V4-Pro，工具在 tools/bench_llm_perf.py）：
  - 健康检查 OK；TTFT ≈ 0.9-1.5s
  - 流式吞吐稳定 ≈ 24 tok/s/请求（生成段）
  - 单章（1500-3000 字, ~1700 tok）≈ 70-75s 流式；非流式 31.6s/760tok
  - meta JSON 任务 31.9s/707tok
  - 并行2路：加速比 1.59x，聚合 38 tok/s；并行3路：加速比 2.65x，聚合 63 tok/s → 服务端基本公平调度，3并行接近线性，无需担心 scene_writers=3 蹭限流
  - 429 有指数退避；非 429 的 4xx/5xx 立即终止重试（好：失败快、不白等）

## Bug 修复（4 个，全绿 345/345）

1. **api/stream.py:146** — agent-events SSE 的 `WorkLog(kd)` 传入的是目录而非文件路径，改为 `WorkLog(kd / "story" / "agent_worklog.jsonl")` 与 orchestrator 写入侧对齐。此前该端点永远推不出 agent 活动。
2. **deai/engine.py:215** — LLM 重写兜底查的是 `"ERROR"` 前缀，但 LLMClient 真实哨兵是 `"[LLM API error"`，永远匹配不上 → 错误文本会被当重写结果混进正文。改为导入 `LLM_ERROR_PREFIX` 校验。
3. **jobs.py:129-189** — `__init__` 里 `asyncio.create_task`，无运行中事件循环时（CLI/脚本/测试同步上下文 new JobRunner）直接抛 RuntimeError。改为登记 `_pending_recover` + `start_pending_recoveries()` 延迟启动；有循环时行为不变，`submit()` 里兜底补启动。
4. **agents/llm_client.py** — 非超时的 TransportError（连接重置/网络抖动）落到 generic except 直接判死，一次瞬时断连就废整章。chat() 与 _stream_chat() 均加短退避重试（2s→30s）；流式侧引入 yielded_any：首 token 前可安全重试，已产出内容后流中断则终止（避免重试造成正文重复）。
- 新增 tests/test_bugfix_regressions.py（9 个用例），全套 345 通过。
